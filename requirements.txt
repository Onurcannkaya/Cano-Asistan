# Cano Sesli Asistan — Bağımlılıklar (Mobil Uyumlu v4.0)
# Kurulum: pip install -r requirements.txt

flet>=0.80.0
google-genai>=1.0.0
edge-tts>=6.1.0
schedule>=1.2.0

# Android yerel bildirimler (GitHub'dan kur):
# pip install git+https://github.com/Bbalduzz/flet_notifications.git
# Not: Bu paket PyPI'da yok, GitHub'dan kurulmalı.
# Masaüstünde opsiyonel — yoksa graceful fallback ile çalışır.
